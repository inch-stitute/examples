#include <stdio.h>
#include <stdlib.h>

void pair_impair(int v) {
  /*
  si v est pair, écrire "pair" sur la sortie standard.
  si v est impair, écrire "impair" sur la sortie standard.
  */
  return;
}

// ne pas toucher pour le moment
int main(int argc, char *argv[]) {
  int k;
  printf("Entrez une valeur entiere : ");
  scanf("%d", &k);
  pair_impair(k);
  return EXIT_SUCCESS;
}
