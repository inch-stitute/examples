#include <stdio.h>
#include <stdlib.h>

void table_mul(int n) {
  /*
  écrire ici la table de multiplication de n, de 1 à 10.
  note: la procédure ne retourne rien.
  */
  return;
}

// ne pas toucher pour le moment
int main(int argc, char *argv[]) {
  int k;
  printf("Entrez une valeur entiere : ");
  scanf("%d", &k);
  table_mul(k);
  return EXIT_SUCCESS;
}
